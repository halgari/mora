namespace: form
# ARMO record (TESObjectARMO). DNAM carries armorRating as float32*100.
relations:
  armor:
    type: predicate
    args: [{name: F, type: FormRef}]
    source: static
    esp: {record_type: ARMO}
    docs: "True when F is an armor base record."
