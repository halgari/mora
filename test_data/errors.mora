namespace test.errors

# Intentionally-broken fixture used to verify diagnostics.
# The smoke test skips this file.

wrong(NPC):
    form/npc(NPC, @Extra)

also_wrong(NPC):
    form/nonexistent_fact(NPC)
