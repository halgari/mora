Exercises every parser-supported armor trait predicate: armor type,
body slot number, armor rating range, weight range, unenchanted
filter. Counterpart to `traits_weapon_all`. (Value predicate exists in
KID's grammar but isn't in mora-kid's parser yet.)
