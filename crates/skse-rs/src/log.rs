//! File-based logger for SKSE plugins. Populated in Phase E (Task 10).

/// Stub placeholder — real impl + variants arrive in Task 10.
#[derive(Debug, thiserror::Error)]
pub enum LogInitError {
    #[error("log init error (stub)")]
    Stub,
}
